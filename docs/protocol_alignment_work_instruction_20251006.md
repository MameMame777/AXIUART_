# 🔍 AXIUART_ プロトコル整合性確立作業指示書

**作成日**: 2025年10月6日  
**対象プロジェクト**: AXIUART_ UART-AXI4 Lite Bridge System  
**作業目的**: RTL実装とプロトコル仕様の完全整合性確立

---

## 📋 作業概要

### 現状認識
- **ハードウェア**: 正常動作（通信・レジスタアクセス機能）
- **問題**: プロトコルレベルでの値不整合（SOF: 0x5A→0x2D, STATUS: 0x00→0x6C）
- **影響**: システム統合テスト停滞、開発プロセス阻害

### 作業方針
1. **RTL実装の詳細仕様把握** - 実際の動作を完全に理解
2. **プロトコル仕様の正確な把握** - 設計意図と仕様書の整合性確認
3. **デバッグ方針の確立** - 効果的な問題解決アプローチの策定

---

## 🎯 Phase 1: RTL実装仕様の詳細把握

### 1.1 プロトコル定数の完全調査

**目標**: RTL内で実際に使用されている全プロトコル定数の特定

#### 1.1.1 SOF定数の調査
- [ ] **1.1.1.1** 全RTLファイルでのSOF関連定数検索
  ```bash
  # 実行コマンド
  grep -r "SOF\|0x5A\|0x2D" rtl/ --include="*.sv" > sof_constants_analysis.txt
  ```
  
- [ ] **1.1.1.2** Frame_Builder.svでのSOF送信ロジック確認
  ```systemverilog
  // 確認箇所
  localparam [7:0] SOF_DEVICE_TO_HOST = ?;
  tx_fifo_data = ?; // SOF送信時の実際の値
  ```

- [ ] **1.1.1.3** Frame_Parser.svでのSOF受信ロジック確認
  ```systemverilog
  // 確認箇所  
  localparam [7:0] SOF_HOST_TO_DEVICE = ?;
  // SOF検出条件と期待値
  ```

#### 1.1.2 STATUS定数の調査
- [ ] **1.1.2.1** 全RTLファイルでのSTATUS関連定数検索
  ```bash
  # 実行コマンド
  grep -r "STATUS\|0x00\|0x6C" rtl/ --include="*.sv" > status_constants_analysis.txt
  ```

- [ ] **1.1.2.2** AXI Master HandlerでのSTATUS生成確認
  ```systemverilog
  // 確認箇所
  localparam [7:0] STATUS_OK = ?;
  assign axi_status = ?; // 実際の値
  ```

- [ ] **1.1.2.3** Bridge経由でのSTATUS伝播確認
  ```systemverilog
  // 確認箇所
  builder_status_code = ?; // Bridgeから Frame_Builderへの値
  ```

#### 1.1.3 CMD_ECHO処理の調査
- [ ] **1.1.3.1** コマンドキャプチャ・エコーロジック確認
  ```systemverilog
  // 確認箇所
  captured_cmd <= parser_cmd; // キャプチャ処理
  builder_cmd_echo = captured_cmd; // エコー処理
  ```

**成功基準**: RTL内の全プロトコル定数と処理ロジックの完全なマッピング作成

### 1.2 データフロー詳細解析

**目標**: 送信データの完全なフロー追跡

#### 1.2.1 Frame Builder内データフロー
- [ ] **1.2.1.1** 入力から送信までの詳細フロー
  ```
  Input Values → Internal Registers → Correction Logic → TX FIFO → UART TX
  ```

- [ ] **1.2.1.2** 修正マスク適用箇所の特定
  ```systemverilog
  // 各フィールドでの修正適用確認
  SOF: tx_fifo_data = ? ^ ?;
  STATUS: tx_fifo_data = ? ^ ?;  
  CMD: tx_fifo_data = ? ^ ?;
  ```

#### 1.2.2 UART送信経路データフロー
- [ ] **1.2.2.1** TX FIFOからUART TXまでの詳細フロー
  ```systemverilog
  TX FIFO → rd_data → uart_tx_data → tx_shift_reg → uart_tx_pin
  ```

- [ ] **1.2.2.2** シリアル送信ビット順序確認
  ```systemverilog
  // LSB first送信の確認
  uart_tx_int = tx_shift_reg[0]; // bit 0 → bit 7の順序
  ```

**成功基準**: データが変換される正確な箇所と機構の特定

### 1.3 状態管理ロジック解析

**目標**: プロトコル処理の状態依存性確認

#### 1.3.1 Frame Builder状態機械
- [ ] **1.3.1.1** 状態遷移とデータ送信タイミング
  ```systemverilog
  IDLE → SOF → STATUS → CMD → DATA → CRC → IDLE
  各状態での tx_fifo_data 値の確認
  ```

- [ ] **1.3.1.2** 状態間でのデータ保持・変更確認
  ```systemverilog
  // レジスタ値の保持期間と変更タイミング
  status_reg, cmd_reg の状態依存性
  ```

#### 1.3.2 Bridge状態管理
- [ ] **1.3.2.1** メイン状態とbuilder制御の関係
  ```systemverilog
  MAIN_IDLE → MAIN_BUILD_RESPONSE → MAIN_WAIT_RESPONSE
  各状態での builder_* 信号の値
  ```

**成功基準**: 状態依存のデータ変換メカニズムの解明

---

## 📚 Phase 2: プロトコル仕様の完全把握

### 2.1 プロトコル仕様書の詳細解析

**目標**: 設計意図とプロトコル定義の正確な理解

#### 2.1.1 仕様書での定数定義確認
- [ ] **2.1.1.1** uart_axi4_protocol.md での定数定義
  ```markdown
  # 確認項目
  - SOF (Start of Frame) 定義
  - STATUS コード定義 
  - フレーム構造定義
  - エラーハンドリング定義
  ```

- [ ] **2.1.1.2** プロトコルバージョンと改訂履歴
  ```markdown
  # 確認項目
  - 最新バージョン番号
  - 改訂内容と変更理由
  - 下位互換性の有無
  ```

#### 2.1.2 フレーム構造の詳細仕様
- [ ] **2.1.2.1** 送信フレーム（Host → Device）
  ```
  SOF[1] + CMD[1] + ADDR[4] + DATA[0-n] + CRC[1]
  各フィールドの値と意味の確認
  ```

- [ ] **2.1.2.2** 応答フレーム（Device → Host）
  ```
  SOF[1] + STATUS[1] + CMD_ECHO[1] + DATA[0-n] + CRC[1]
  各フィールドの期待値の確認
  ```

#### 2.1.3 エラーコード体系の把握
- [ ] **2.1.3.1** STATUS値の完全定義
  ```c
  #define UART_AXI_OK              0x00    // Success
  #define UART_AXI_ERR_CRC         0x01    // CRC mismatch
  #define UART_AXI_ERR_CMD_INV     0x02    // Invalid command
  // ... 全エラーコードの把握
  ```

**成功基準**: プロトコル仕様書の完全理解とRTL実装との差分明確化

### 2.2 設計文書との整合性確認

**目標**: システム設計意図の正確な把握

#### 2.2.1 システムアーキテクチャ確認
- [ ] **2.2.1.1** design_overview.md の設計意図確認
  ```markdown
  # 確認項目
  - システム全体アーキテクチャ
  - コンポーネント間インタフェース
  - プロトコル実装方針
  ```

- [ ] **2.2.1.2** register_map.md でのレジスタ定義確認
  ```markdown
  # 確認項目
  - テストレジスタ（0x00001020-0x0000102C）の仕様
  - レジスタアクセス方法
  - 期待される動作
  ```

#### 2.2.2 実装履歴の調査
- [ ] **2.2.2.1** 開発日記での実装変更履歴確認
  ```markdown
  # 確認対象ファイル
  - diary_*.md での実装変更内容
  - protocol_violation_* での問題発生経緯
  - 修正試行の履歴と結果
  ```

**成功基準**: 設計意図とプロトコル仕様の整合性確認

### 2.3 テストケース仕様の確認

**目標**: テスト期待値の妥当性検証

#### 2.3.1 UVMテストベンチ仕様
- [ ] **2.3.1.1** uvm_verification_plan.md での期待値確認
  ```systemverilog
  // 確認項目
  - UVMテストでの期待SOF値
  - UVMテストでの期待STATUS値
  - テストシナリオの妥当性
  ```

#### 2.3.2 Pythonテストスクリプト仕様
- [ ] **2.3.2.1** test_registers_updated.py での期待値確認
  ```python
  # 確認項目
  EXPECTED_SOF = ?          # 期待するSOF値
  EXPECTED_STATUS_OK = ?    # 期待するSTATUS値
  # テスト手順の妥当性
  ```

**成功基準**: テスト仕様とプロトコル仕様の整合性確認

---

## 🛠️ Phase 3: デバッグ方針の確立

### 3.1 デバッグ手法の選定

**目標**: 効果的なデバッグアプローチの確立

#### 3.1.1 RTL内部観測手法
- [ ] **3.1.1.1** ILA (Integrated Logic Analyzer) 活用
  ```tcl
  # 観測対象信号
  - Frame_Builder: tx_fifo_data, state, debug_*
  - Bridge: builder_status_code, captured_cmd
  - UART TX: tx_data, tx_shift_reg, uart_tx
  ```

- [ ] **3.1.1.2** SystemVerilog Debug機能活用
  ```systemverilog
  `ifdef ENABLE_DEBUG
      $display("DEBUG: 実際の送信値 = 0x%02X", tx_fifo_data);
  `endif
  ```

#### 3.1.2 プロトコルレベル解析手法
- [ ] **3.1.2.1** UARTプロトコルアナライザー活用
  ```
  # 解析項目
  - 実際のUART波形キャプチャ
  - ビットレベルでの送信内容確認
  - タイミング解析
  ```

- [ ] **3.1.2.2** Pythonログ拡張
  ```python
  # 強化項目
  - 送受信バイトの詳細ログ
  - ビットパターン解析
  - 統計的傾向分析
  ```

### 3.2 段階的デバッグ戦略

**目標**: 効率的な問題解決手順の確立

#### 3.2.1 最小構成での検証
- [ ] **3.2.1.1** 単一レジスタアクセステスト
  ```python
  # 最小テストケース
  1. 1レジスタのみ読み取り
  2. 1レジスタのみ書き込み  
  3. 各フィールドの個別確認
  ```

- [ ] **3.2.1.2** NOPコマンドテスト（可能であれば）
  ```python
  # プロトコル最小テスト
  - データ転送なしのコマンド
  - フレーム構造のみの確認
  ```

#### 3.2.2 比較検証手法
- [ ] **3.2.2.1** UVMテストとPythonテストの比較
  ```
  # 比較項目
  - 同一コマンドでの応答値比較
  - テスト環境差による影響確認
  - 期待値設定の妥当性検証
  ```

- [ ] **3.2.2.2** 複数ボーレートでの動作確認
  ```python
  # テスト条件
  - 115200 baud（現在設定）
  - 9600 baud（低速・安定性重視）
  - 動作差による問題要因特定
  ```

### 3.3 問題解決アプローチ

**目標**: 根本解決に向けた体系的アプローチ

#### 3.3.1 仮説検証型アプローチ
- [ ] **3.3.1.1** 仮説1: RTL定数定義相違
  ```
  検証方法: RTL内全定数の詳細調査
  判定基準: 仕様書と異なる定数の発見
  ```

- [ ] **3.3.1.2** 仮説2: プロトコル仕様解釈相違
  ```
  検証方法: 設計文書とテスト仕様の比較
  判定基準: 期待値設定の根拠確認
  ```

- [ ] **3.3.1.3** 仮説3: 実装バグ（状態依存変換）
  ```
  検証方法: ILAによる状態遷移詳細観測
  判定基準: 予期しない状態依存動作の発見
  ```

#### 3.3.2 解決策優先順位付け
- [ ] **3.3.2.1** 短期解決策（開発継続優先）
  ```
  1. 現在値をベースライン化
  2. テストスクリプト調整
  3. 暫定的なプロトコル統一
  ```

- [ ] **3.3.2.2** 長期解決策（根本修正）
  ```
  1. RTL実装修正
  2. プロトコル仕様統一
  3. 検証プロセス改善
  ```

---

## 📅 作業スケジュールと成果物

### Phase 1: RTL仕様把握（2日間）
**成果物**:
- `rtl_specification_analysis_20251006.md` - RTL実装詳細仕様書
- `protocol_constants_mapping.csv` - 定数対応表
- `data_flow_diagram.md` - データフロー詳細図

### Phase 2: プロトコル仕様把握（1日間）
**成果物**:
- `protocol_specification_analysis_20251006.md` - プロトコル仕様詳細解析書
- `specification_gap_analysis.md` - RTLと仕様の差分解析
- `test_specification_review.md` - テスト仕様妥当性評価

### Phase 3: デバッグ方針確立（1日間）
**成果物**:
- `debug_strategy_plan_20251006.md` - デバッグ戦略計画書
- `verification_procedure.md` - 検証手順書
- `problem_resolution_roadmap.md` - 問題解決ロードマップ

### 最終統合（0.5日間）
**成果物**:
- `protocol_alignment_solution_20251006.md` - 統合解決策
- `implementation_plan.md` - 実装計画書

---

## ✅ 品質保証基準

### 文書品質
- [ ] 全調査結果の客観的記録
- [ ] 再現可能な検証手順の記載
- [ ] 明確な判断基準と根拠の提示

### 技術品質
- [ ] RTL実装の完全理解
- [ ] プロトコル仕様の正確な把握
- [ ] 実証に基づく問題解決策

### プロジェクト品質
- [ ] 開発継続への即効性
- [ ] 根本解決への道筋明確化
- [ ] 再発防止策の確立

---

## 🎯 最終目標

**プロトコル完全整合性の確立により:**
- ✅ システム統合テストの正常化
- ✅ 開発プロセスの円滑な継続
- ✅ 高品質なUART-AXI4ブリッジシステムの実現

*SystemVerilogエンジニアとして、論理的で体系的なアプローチにより*  
*確実な問題解決を実現します。* 🎯